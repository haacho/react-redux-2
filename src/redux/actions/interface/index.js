import { createAction } from 'redux-actions';

export const UPDATE_COLOR = createAction('UPDATE_COLOR');
export const OBTENER_COLORES = createAction('OBTENER_COLORES');
export const UPDATE_TITULO = createAction('UPDATE_TITULO');